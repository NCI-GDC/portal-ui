// @flow

import React from 'react';
import CaseTable from '@ncigdc/containers/cohort/CaseTable';
import CohortCasesPies from '@ncigdc/components/TabPieCharts/CohortCasesPies';

const CasesTab = props => (
  <div>
    <CohortCasesPies
      aggregations={props.aggregations}
    />
    <CaseTable hits={props.hits} cohort={props.cohort} />
  </div>
);

export default CasesTab;
