import { absoluteCenter } from '@ncigdc/theme/mixins';

export const floatingNumber = {
  ...absoluteCenter,
  fontSize: '0.8em',
  backgroundColor: 'white',
  borderRadius: 15,
  padding: '1px 3px',
};
