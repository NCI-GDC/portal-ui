// @flow
import React from 'react';

import Button from '@ncigdc/uikit/Button';

type TProps = {
  closeModal: Function,
};

const NoAccessModal = ({
  closeModal,
}: TProps) => (
  <div>
    jeffrey's making this, overwrite on merge
    <Button onClick={closeModal}>
      Cancel
    </Button>
  </div>
);

export default NoAccessModal;
