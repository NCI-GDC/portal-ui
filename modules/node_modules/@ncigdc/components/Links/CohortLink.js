/* @flow */
import React from 'react';
// import { makeListLink } from './utils';

// export const CohortMutationsLink = makeListLink({
//   pathname: '/exploration',
//   children: 'exploration',
//   query: { searchTableTab: 'mutations' },
// });

// export default makeListLink({ pathname: '/exploration', children: 'exploration' });

// temporarily disable all CohortLinks
export default ({ children, style }) => <span style={style}>{children}</span>;
