/* @flow */

export type TTableProps = {
  downloadable?: boolean,
  hits: {
    edges: Array<{|
      node: {|
       id: string,
     |},
   |}>,
    total: number,
  },
  relay: {
    route: {
      params: Object,
    },
  },
  canAddToCart?: boolean,
  tableHeader?: string,
};
