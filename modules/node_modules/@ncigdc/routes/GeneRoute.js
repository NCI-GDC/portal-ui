/* @flow */
/* eslint fp/no-class:0 */

import React from 'react';
import Relay from 'react-relay';

import GenePage from '@ncigdc/containers/GenePage';
import { prepareNodeParams } from '@ncigdc/utils/routes';

import { nodeQuery } from './queries';

class GeneRoute extends Relay.Route {
  static routeName = 'FilePageRoute';
  static queries = nodeQuery;
  static prepareParams = prepareNodeParams('Gene');
}

export default (props: mixed) => (
  <Relay.Renderer
    Container={GenePage}
    queryConfig={new GeneRoute(props)}
    environment={Relay.Store}
  />
);
