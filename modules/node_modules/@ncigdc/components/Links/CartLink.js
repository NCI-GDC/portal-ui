/* @flow */

import React from 'react';
import Link from './Link';

type TProps = {|
  children?: mixed,
|};

const CartLink = (props: TProps) => (
  <Link pathname="/cart">{props.children || 'cart'}</Link>
);

export default CartLink;
