/* @flow */

export type TTableProps = {
  downloadable?: boolean,
  hits: {
    edges: Array<{|
      node: {|
       id: string,
     |},
   |}>,
    pagination: {
      offset: number,
      size: number,
      sort: string,
      total: number,
    },
  },
  relay: {
    route: {
      params: Object,
    },
  },
  isCartPage: Boolean,
};
